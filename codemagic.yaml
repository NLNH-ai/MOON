workflows:
  ios-simulator-prototype:
    name: OneulDal iOS Simulator Prototype
    instance_type: mac_mini_m2
    max_build_duration: 30
    environment:
      xcode: latest
      vars:
        XCODE_PROJECT: OneulDal.xcodeproj
        XCODE_SCHEME: OneulDal
    scripts:
      - name: Validate project files
        script: |
          set -euo pipefail
          python3 - <<'PY'
          import json
          import plistlib
          from pathlib import Path

          root = Path(".")
          required = [
              "OneulDal.xcodeproj/project.pbxproj",
              "OneulDal.xcodeproj/xcshareddata/xcschemes/OneulDal.xcscheme",
              "OneulDal/OneulDalApp.swift",
              "OneulDal/AppRootView.swift",
              "OneulDal/AppModel.swift",
              "OneulDal/AstronomyService.swift",
              "OneulDal/NotificationService.swift",
              "OneulDal/MoonRendering.swift",
              "OneulDal/MoonLighting.metal",
              "OneulDal/TodayView.swift",
              "OneulDal/MoonCalendarView.swift",
              "OneulDal/MoonPhotoModeView.swift",
              "OneulDal/MoonNotificationsView.swift",
              "OneulDal/Assets.xcassets/MoonSurface.imageset/moon-surface.png",
              "OneulDal/PrivacyInfo.xcprivacy",
              "OneulDalTests/OneulDalTests.swift",
          ]
          missing = [path for path in required if not (root / path).exists()]
          if missing:
              raise SystemExit(f"Missing required files: {missing}")

          for path in (root / "OneulDal/Assets.xcassets").glob("**/*.json"):
              json.loads(path.read_text(encoding="utf-8"))
          plistlib.loads((root / "OneulDal/Info.plist").read_bytes())
          plistlib.loads((root / "OneulDal/PrivacyInfo.xcprivacy").read_bytes())
          print("Project files, asset catalogs, Info.plist, and PrivacyInfo.xcprivacy are present.")
          PY
      - name: Resolve Swift packages
        script: |
          set -euo pipefail
          xcodebuild -resolvePackageDependencies \
            -project "$XCODE_PROJECT" \
            -scheme "$XCODE_SCHEME"
      - name: Ensure Metal toolchain
        script: |
          set -euo pipefail
          xcodebuild -downloadComponent MetalToolchain
      - name: Pick available iPhone simulator
        script: |
          set -euo pipefail
          SIMULATOR_NAME="${SIMULATOR_NAME:-$(xcrun simctl list devices available | awk -F '[()]' '/iPhone/{gsub(/^[ \t]+|[ \t]+$/, "", $1); print $1; exit}')}"
          if [ -z "$SIMULATOR_NAME" ]; then
            echo "No available iPhone simulator found."
            exit 1
          fi
          echo "Using simulator: $SIMULATOR_NAME"
          echo "SIMULATOR_NAME=$SIMULATOR_NAME" >> "$CM_ENV"
      - name: List schemes
        script: |
          set -euo pipefail
          xcodebuild -list -project "$XCODE_PROJECT"
      - name: Build for simulator
        script: |
          set -euo pipefail
          mkdir -p "$CM_BUILD_DIR/build"
          xcodebuild build-for-testing \
            -project "$XCODE_PROJECT" \
            -scheme "$XCODE_SCHEME" \
            -destination "platform=iOS Simulator,name=$SIMULATOR_NAME,OS=latest" \
            -derivedDataPath "$CM_BUILD_DIR/build/DerivedData" \
            CODE_SIGNING_ALLOWED=NO
      - name: Run unit tests
        script: |
          set -euo pipefail
          xcodebuild test-without-building \
            -project "$XCODE_PROJECT" \
            -scheme "$XCODE_SCHEME" \
            -destination "platform=iOS Simulator,name=$SIMULATOR_NAME,OS=latest" \
            -derivedDataPath "$CM_BUILD_DIR/build/DerivedData" \
            -resultBundlePath "$CM_BUILD_DIR/build/OneulDalTests.xcresult" \
            CODE_SIGNING_ALLOWED=NO
    artifacts:
      - build/DerivedData/Build/Products/Debug-iphonesimulator/*.app
      - build/*.xcresult
      - /tmp/xcodebuild_logs/*.log

  ios-testflight:
    name: OneulDal iOS TestFlight Internal
    instance_type: mac_mini_m2
    max_build_duration: 60
    integrations:
      app_store_connect: oneuldal-app-store-connect
    environment:
      xcode: latest
      vars:
        APP_VERSION: "1.0.0"
        BUNDLE_ID: "com.oneuldal.app"
        XCODE_PROJECT: OneulDal.xcodeproj
        XCODE_SCHEME: OneulDal
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.oneuldal.app
    triggering:
      events:
        - tag
      tag_patterns:
        - pattern: "ios-*"
          include: true
    scripts:
      - name: Validate release environment
        script: |
          set -euo pipefail
          xcodebuild -version
          xcodebuild -downloadComponent MetalToolchain
          xcodebuild -list -project "$XCODE_PROJECT"
          xcodebuild -resolvePackageDependencies \
            -project "$XCODE_PROJECT" \
            -scheme "$XCODE_SCHEME"
      - name: Prepare release build settings
        script: |
          set -euo pipefail
          python3 - <<'PY'
          import os
          import re
          from pathlib import Path

          project_path = Path("OneulDal.xcodeproj/project.pbxproj")
          project = project_path.read_text(encoding="utf-8")
          bundle_id = os.environ["BUNDLE_ID"]
          app_version = os.environ.get("APP_VERSION", "1.0.0")
          build_number = os.environ.get("IOS_BUILD_NUMBER") or os.environ.get("BUILD_NUMBER") or "1"

          project = re.sub(r"CURRENT_PROJECT_VERSION = [^;]+;", f"CURRENT_PROJECT_VERSION = {build_number};", project)
          project = re.sub(r"MARKETING_VERSION = [^;]+;", f"MARKETING_VERSION = {app_version};", project)
          project = re.sub(
              r"PRODUCT_BUNDLE_IDENTIFIER = (?:com\.codex\.oneuldal|com\.oneuldal\.app);",
              f"PRODUCT_BUNDLE_IDENTIFIER = {bundle_id};",
              project,
          )
          project = re.sub(
              r"PRODUCT_BUNDLE_IDENTIFIER = (?:com\.codex\.oneuldal\.tests|com\.oneuldal\.app\.tests);",
              f"PRODUCT_BUNDLE_IDENTIFIER = {bundle_id}.tests;",
              project,
          )
          project_path.write_text(project, encoding="utf-8")
          print(f"Prepared {bundle_id} version {app_version} build {build_number}.")
          PY
      - name: Set up App Store code signing
        script: |
          set -euo pipefail
          xcode-project use-profiles \
            --project "$CM_BUILD_DIR/$XCODE_PROJECT"
      - name: Build IPA for TestFlight
        script: |
          set -euo pipefail
          xcode-project build-ipa \
            --project "$CM_BUILD_DIR/$XCODE_PROJECT" \
            --scheme "$XCODE_SCHEME"
    artifacts:
      - build/ios/ipa/*.ipa
      - $HOME/export_options.plist
      - /tmp/xcodebuild_logs/*.log
      - $HOME/Library/Developer/Xcode/DerivedData/**/Build/**/*.dSYM
    publishing:
      app_store_connect:
        auth: integration
